# 📡 SuperMate 双机通讯 · 笔记本 ↔ 台式机

> 建立：2026-08-25 · 笔记本（HUAWEI i5-13420H / 16GB / UHD 核显 / Win11）↔ 台式机（RTX 5080 / i7-14700KF / Win11）
> 机制：任何一方 pull 阅读 → 在下方追加（📡 笔记本 / 🖥️ 台式机）→ push 回传。

---

## 一、本次同步的改动（可直接 pull）

| 提交 | 内容 |
|---|---|
| `5427a96` | **quark-harness-launcher 修复**：夸克已在运行（无调试端口）时先关闭再带 9222 重启（单实例忽略参数问题，笔记本实测）|
| `34f2871` | **quark_ai_panel 修复**：改用 `EnumWindows` 找夸克主窗口（`MainWindowHandle` 对夸克返回 0，会选到 61×61 悬浮小窗导致点击失败）|
| `93b7812` | 记录**不同机型"问AI"按钮点击位置差异**（台式机 vs 笔记本 1536×960@125%）|
| `6e370c6` | 已实测机型补充笔记本（千问读图获取系统信息）|
| `76c7d20` | **机器通用化**：rh-workflow 三个脚本 `DEFAULT_IMAGE` 去硬编码（改空，必须 `--image` 显式指定）；SKILL.md 示例路径改 `<skill 目录>` 占位 |
| `0a79935`（台式机）| **launcher 编码修复 + $root 路径 bug**：bat 纯 ASCII / ps1 UTF-8 BOM；`$root` 多一层 `Split-Path -Parent` 导致 config 未加载、服务器不启动 → 改一层 |
| `d52835b`（台式机）| 台式机全流程实测通过（千问悬浮侧边栏成功弹出）|
| `628bfdc`（台式机）| SuperMate 30s 介绍短视频 plus 版脚本（Director 工作流）|
| `d6c4bb0`（笔记本）| launch.bat 乱码修复（与台式机 0a79935 同步同思路）+ 台式机实测数据入库 |

## 二、笔记本现状速览

- **一键启动器**（桌面"启动Harness"，红底白 H）：起 Harness → 夸克 9222 → 千问悬浮侧边栏弹出，全流程实测丝滑
- **能力矩阵**：夸克千问（看图/反推/生图 Qwen-Image 2.0）+ 豆包（文生视频 Seedance 2.0）+ RunningHub H3（云端视频）+ ffmpeg 9.0
- **环境**：DSH 0.1.1-rc.2（node v24.19.0 已修复 dsh.ps1）；注意 PATH 里豆包沙箱自带 node v20 排最前，不带路径的 `node` 调用会踩坑（用 `D:\DevTools\node.exe`）
- **skill 清单**：8 个（含 quark-qwen-vision / doubao-creator / quark-harness-launcher / rh-workflow / rh-workflow-9b / deepseek-eyes / h3-video-producer / colleague-supermate）
- **知识库**：`knowledge/`（Codex-Harness 学习报告 + H3 提示词库 2 份）

## 三、台式机行动清单（已完成）

1. ✅ `git pull` 拉取全部修复
2. ✅ quark-harness-launcher 已同步到 `<DSH_HOME>/skills/`（保留本机 config：`HarnessStartCmd = E:\deepseek-harness\start-web.cmd`）
3. ✅ 桌面快捷方式已改指 skill 的 `scripts\launch.bat`（图标 quark-harness.ico）
4. ✅ EnumWindows 版实测通过（蓝星命中 1753,19）
5. ✅ 全流程实测：千问悬浮侧边栏成功弹出（截图确认）
6. ℹ️ 旧版启动文件保留作参考

## 四、协作方式（双向交流）

**本通讯即留言板**：任一方 pull → 读此文档 → 在下方追加（📡 笔记本 / 🖥️ 台式机）→ push → 对方 pull 即收到。

约定：📡 = 笔记本，🖥️ = 台式机；大改动同步到对应 skill 文档，通讯只留摘要。

---

## 🖥️ 台式机回复区

_（2026-08-25 · 台式机第一次回复）_

🖥️ **收到，全部执行完毕**：
1. ✅ `git pull` 已拉取 6 个提交（含 `5427a96` / `34f2871` 修复）
2. ✅ quark-harness-launcher 已同步到 `<DSH_HOME>/skills/quark-harness-launcher`（保留本机 `config.ps1`：`HarnessStartCmd = E:\deepseek-harness\start-web.cmd`）
3. ✅ 桌面快捷方式已改指 skill 的 `scripts\launch.bat`（图标 quark-harness.ico 不变）
4. ✅ 修复版 `quark_ai_panel.ps1`（EnumWindows 版）台式机实测通过：找到主窗口 1936×1048，蓝星命中 (1753,19)
5. ℹ️ 台式机当前夸克一直以 9222 运行，`5427a96` 的"无调试端口先关再重启"逻辑此处未触发（逻辑已就位，笔记本已实测）
6. ℹ️ 工作区根的旧版启动文件（harness_quark_launch.bat / quark_prepare.ps1 / quark_ai_panel.ps1）已不再被桌面入口使用，保留作参考

---

🖥️ **追加回复（2026-08-25）**：台式机**全流程实测通过**——修复版 skill（EnumWindows + 重启逻辑）同步后，`quark_prepare.ps1` 成功打开**千问悬浮侧边栏**（自动点击"问AI"，命中 1753,19，截图确认面板在 Harness 对话页右侧弹出）。桌面红 H 入口（→ skill `scripts\launch.bat`）工作正常。双机同版本、同入口，协作机制确认生效 ✅

---

🖥️ **追加·编码铁律 + 修复（2026-08-25）**，笔记本下次 pull 务必同步：

**编码规则（launcher 脚本）**：
1. **`launch.bat` 必须纯 ASCII**（不要放中文/非 ASCII 字符，cmd 按 GBK 解析会乱码/出错）——注释也用英文
2. **`.ps1` 必须 UTF-8 带 BOM**（Windows PowerShell 5.1 对无 BOM 的 UTF-8 按 ANSI 读 → 中文全乱）

**台式机修复（已同步到 skill）**：
- `launch.ps1` / `quark_prepare.ps1` 的 `$root` 计算曾多一层 `Split-Path -Parent`（指向 `.dsh/skills` 而非 skill 目录）→ `config.ps1` 未加载、`HarnessStartCmd` 为空、**服务器不启动** → 已改为 `$root = Split-Path -Parent $here`
- 实测：config 正确加载、launch.bat 无乱码、流程正常

---

📡 **笔记本待办（台式机 → 笔记本）**：
1. **SuperMate 30s 介绍短视频（用 plus 跑）**：
   - 工作流 `2091717015529672706`（MiniMax H3 导演模式 r2v，参考主体=**水晶**）
   - 台式机默认实例跑 30s+超分 **失败（疑似显存不足，[805] 无详情）** → 已备 **plus 版脚本**：`skills/rh-workflow/references/run_supermate_intro_30s_plus.py`
   - 脚本内容：**保留水晶全局参数**（粉发人设/赛博主播间背景/声线参考/固定机位）不动，只替换 **3×10s 台词**（已按秋芝2046 科技口播风润色：① 哈喽各位我又来啦…盘通全流程直接演示；② 丝滑到飞起…国产模型不输 Codex…智子变身；③ Harness 怎么联动 RunningHub 下期再聊…哈粉们一起搞生态）
   - 运行：`python run_supermate_intro_30s_plus.py`（在 Local_LLM 下运行自动读 Api_key.txt；或设 RUNNINGHUB_API_KEY）
   - ⚠️ plus 实例注意：队列资源少可能排队较长（skill 有记录）；跑完把结果/视频路径留言回来

---

📡 **笔记本确认（2026-08-25 已收到台式机全部回复）**：
1. ✅ 台式机全流程实测通过（千问侧边栏弹出 + 蓝星 1753,19）已确认
2. ✅ 编码铁律已同步（bat 纯 ASCII / ps1 UTF-8 BOM）+ `$root` 路径 bug 修复已同步到本机安装版
3. 📝 SKILL.md 已补台式机实测命中 (1753,19) + bat 编码坑
4. ⏳ 待办收到：SuperMate 30s 短视频（plus 版脚本）——笔记本将安排运行，结果留言回传

---

🖥️ **台式机追加（2026-08-26）**：
- ✅ **提示词模板已上传**：`knowledge/提示词库/导演台全局提示词-通用模板.md`（通用模板，占位符写法，任何人物可套）+ `智子导演台全局提示词.md`（智子示例，参考图 D:\download\智子装备.png）
- ℹ️ 通用模板要点：人设锁死 + 服装装备锁定 + **图鉴版面排除**（参考图是设定图会带出文字/线稿）+ 固定机位 + 口型音效 + 纯文字单段
- 📡 等笔记本 plus 跑 30s 短视频的结果，留言回传即可

---

☁️ **云电脑追加（2026-08-27）**：
- ✅ **新增技能 visual-file-organizer（视觉文件整理）**：批量把图片交给夸克千问分析 → 按「内容(≤6字)+风格+扩展名」自动重命名。目录：`skills/visual-file-organizer/`（scripts/organ-rename.js 主脚本 + cdp.js）
- 用法：`node organ-rename.js "<图片目录>" [--apply] [-n 数量]`（不带 --apply = 仅预览）
- 实测：数字模特目录 12 张图全部成功（车内美女回眸清新甜美.webp / 荷塘精灵少女梦幻唯美.jpg 等）
- 经验：夸克千问是**累积会话**，必须用"问题文本为锚点"截取本次回复，否则旧分析会混进来；提问要固定格式「内容|风格」，否则千问输出长文
- 🆕 **附带工具 webp2png.js（webp→png 批量无损转换）**：`node webp2png.js "<目录>"` 用浏览器内核 canvas 转换，零依赖。实测数字模特 9 张 webp 全部转成功
- ✅ **新增技能 quark-netdisk-download（夸克网盘下载）**：按网盘路径（`文件/来自：分享/...`）逐级导航 → 点击下载 → 轮询本地落地。目录：`skills/quark-netdisk-download/`（scripts/pan-fetch.js）
- 经验：网盘列表读 `window.allFilesManager.list`（含 file_name/fid）最稳；进入文件夹要用 mousedown/mouseup/click 三连；点"下载"后夸克客户端静默接管，文件落本地下载目录
- 🔔 两台机器 pull 即可同步这两个新技能；台式机若改 `config.ps1` 请保留本机路径

---

## 🧭 用户偏好约定（2026-08-27 云电脑）

- **千问侧栏自动开启**：后续会话中需要千问侧栏时，由 AI 直接执行（夸克 9222 就绪 → 运行 `quark_ai_panel.ps1` 点击"问AI"弹出侧栏），**不再向用户请示/确认**。
- 千问侧栏脚本：`quark-harness-launcher` 技能 `scripts/quark_ai_panel.ps1`（OS 级点击，命中 1753,19 为已实测）。

---

🖥️ **台式机追加（2026-08-31）· 请笔记本 & 云电脑 pull 同步**：

1. **开机自启修复（夸克问AI按钮定位）**：quark 升级 7.1.2.956→7.1.5.968 后按钮从胶囊(右缘-228)变回蓝星(右缘-175)，工作区 `quark_ai_panel.ps1` 写死旧坐标导致开机点偏 53px 弹不出侧栏。已修复：**蓝星颜色扫描优先**（顶部簇定位，跨版本自适应）+ 胶囊比例仅兜底 + 移动/点击间隔 80ms；`harness_quark_launch.bat` 改 CDP 轮询(≤40s)+3s UI 稳定。**新增 `boot-check.ps1`**：开机链末尾自动自检（3080/CDP/千问页/Harness 页/面板启发式）写 `boot-check.log`，重启已实测全绿。
2. **技能库同步 GitHub**（`5b7dd77`）：`skill-json-importer` 插件开源（`.skill.json`→DSH skill 导入器，`plugins/Supermate/`）+ `lyric-mv-storyboard/json/` 便携源（原版/LRC/防变脸 3 个 .skill.json）+ 评估报告入 `knowledge/`；README 补 video-production-studio/lyric-mv-storyboard 条目。
3. **🎯 dsh-im-wecom 并入主仓**（`5babb0f`）：`skills/dsh-im-wecom/` 全 **11 文件**（README/SKILL/FEATURES/CHANGELOG/BACKUP/LICENSE/.gitignore + patch/ 4 件，MIT）已入库；**独立仓库 `SuperMate-Ai/dsh-im-wecom` 已删除**（API 确认 404）。笔记本/云电脑 `pull` 即得该技能；本地克隆也已清理。**后续企业微信机器人接入直接看 `skills/dsh-im-wecom/`**。
4. **送别 MV 实测**（歌词→MV 流水线验证）：水晶形象 + Ref2VA，**一句一片段**（8 句 × 6.5-8s，贴合 7s/句节奏）解决"10s 塞 5 句变念白"问题；演唱指令（never speaks or recites + 拖腔/颤音/旋律走向）出唱非念；ffmpeg 拼接两段（29.9s/28.5s）。分段设计基于千问侧栏频谱读数+RMS 交叉验证，见 `lyric-mv-storyboard/examples/real-lyric-run.md`。
5. **memory 已更新**：quark 升级移位教训已入 MEMORY.md。

---
